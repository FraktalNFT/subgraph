{
  "name": "fraktal2rinkeby",
  "version": "0.0.3",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ fraktal2rinkeby",
    "create-local": "graph create --node http://localhost:8020/ fraktal2rinkeby",
    "remove-local": "graph remove --node http://localhost:8020/ fraktal2rinkeby",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fraktal2rinkeby",
    "test": "graph test market",
    "coverage": "graph test -- -c",
    "predocker-test": "docker build -t matchstick .",
    "docker-test": "docker run --rm matchstick"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.25.3",
    "@graphprotocol/graph-ts": "^0.24.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.4.0"
  }
}
